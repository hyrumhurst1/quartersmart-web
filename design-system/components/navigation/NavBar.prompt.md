Sticky top navigation with translucent blur, logo, links, and a single CTA.

```jsx
<NavBar current="/services" cta={{ label: "Book a call", href: "/contact#book-a-call" }} />
```

Collapses to a mobile menu below 880px. Keep the CTA to one of the two approved labels ("Book a call" / "Request a proposal").
