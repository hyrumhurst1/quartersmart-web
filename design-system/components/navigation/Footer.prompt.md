Full site footer with a CTA band, link columns, logo blurb, and a meta row.

```jsx
<Footer />
<Footer showCta={false} />  {/* on pages that already end in a CTA */}
```

The CTA band uses both approved labels. Set `showCta={false}` when the page body already closes with a call to action.
