Base surface container for grouping content on dark.

```jsx
<Card padding="lg" interactive>…</Card>
<Card glow>Featured</Card>
```

`interactive` adds hover lift + mint border (use for clickable cards), `glow` for one highlighted card. Compose `FeatureCard` / `CaseStudyCard` on top of it rather than restyling.
