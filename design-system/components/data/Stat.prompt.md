Measured proof point: big mint number with a plain-language label.

```jsx
<Stat value="65%" label="less onboarding time and fewer repeat questions" sublabel="across 7 roles" />
<Stat value="10,000+" label="template uses" align="center" />
```

Sizes `sm` / `md` / `lg`. Keep labels factual and specific; this brand earns trust with verifiable numbers, not hype.
