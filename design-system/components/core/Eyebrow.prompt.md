Small mono uppercase kicker above a heading; sets section context in the brand's "system" voice.

```jsx
<Eyebrow>AI onboarding, training, and automation</Eyebrow>
<h2>One source, two outputs</h2>
```

Default mint `accent` color; use `muted` / `faint` on busy backgrounds.
