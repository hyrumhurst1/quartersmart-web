Pill for credentials, status, or short metadata.

```jsx
<Badge variant="mint" dot>Google Certified</Badge>
<Badge variant="outline">Mesa, Arizona</Badge>
```

Variants: `mint` (tinted, default), `outline` (neutral hairline), `solid` (mint fill). `dot` adds a leading indicator.
