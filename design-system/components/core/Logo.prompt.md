QuarterSmart logo lockup. Inline SVG mark, no asset path needed.

```jsx
<Logo />                    {/* mark + wordmark */}
<Logo wordmark={false} size={40} />  {/* mark only */}
```

The mark is the placeholder four-quarters circle (top-right quarter bright mint). Do not pair it with a letter-Q mark.
